import React from 'react';

const LoginPatient = ({ onLogin }) => {
  return (
    <div>
      <h2>Connexion Patient</h2>
      <p>Formulaire de connexion patient (à implémenter)</p>
    </div>
  );
};

export default LoginPatient;